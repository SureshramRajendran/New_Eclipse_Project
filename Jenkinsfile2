pipeline {
    agent any 
environment{
fname = "Suresh"

}
        stages {
            stage ('one') {
            steps {
                echo "Hello ${fname}"
                    }
            }
            stage ('two') {
            steps {
                echo "Hello ${fname}"
                    }
            }
         
    }
    }
